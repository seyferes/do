
### Фундамент (без этого DevOps “не держится”)

1. **Linux**: файловая система, права, процессы, systemd, пакеты, текстовые утилиты
2. **Сети**: IP, DNS, HTTP/HTTPS, порты, NAT, SSH
3. **Git**: ветки, PR, разрешение конфликтов

### Автоматизация

4. **Bash/Shell**: скрипты, переменные, пайпы, exit codes, cron
5. **CI/CD**: pipeline → тест → билд → пуш образа → деплой

### Контейнеры и оркестрация

6. **Docker**: образы, слои, volumes, сети, registry
7. **Compose**: multi-service, env, healthchecks
8. **Kubernetes**: Pod/Deployment/Service/Ingress, ConfigMap/Secret, HPA basics

### IaC + Cloud

9. **Terraform**: state, modules, resources, variables, outputs
10. **AWS**: IAM, VPC, EC2, S3 (+ базово CloudWatch)

### Наблюдаемость и безопасность

11. **Monitoring/Logging**: Prometheus + Grafana (минимум), логи контейнеров
12. **Security basics**: SSH keys, least privilege IAM, secrets, scanning образов (минимум)

### Архитектурное мышление

13. **“как это работает в проде”**: среда dev/stage/prod, rollback, versioning, SLA/SLI (упрощённо)

