
### Ссылки в Linux — hard links

В Linux файл — это не просто имя, это данные на диске, у которых есть свой номер (индексный дескриптор или **inode**). Имена файлов — это лишь ярлыки, указывающие на эти данные.

Существует два типа ссылок:

1. **Жесткие ссылки (Hard links)** 🔗:
    
    - Это создание еще одного имени для тех же самых данных на диске.
    - Если вы удалите оригинал, файл все равно останется доступен по жесткой ссылке, так как данные удаляются только тогда, когда на них не ссылается ни одно имя.
    - **Ограничение:** нельзя создавать для папок и между разными дисками (файловыми системами).
    - Команда: `ln source_file link_name`
2. **Символьные ссылки (Symlinks / Soft links)** ⛓️:
    
    - Это аналог «ярлыка» в Windows. Это маленький файл, который просто содержит путь к другому файлу.
    - Если удалить оригинал, ссылка «сломается» (будет указывать в никуда).
    - **Плюс:** можно ссылаться на папки и на другие диски.
    - Команда: `ln -s source_file link_name`